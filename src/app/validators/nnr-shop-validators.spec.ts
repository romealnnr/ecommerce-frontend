import { NnrShopValidators } from './nnr-shop-validators';

describe('NnrShopValidators', () => {
  it('should create an instance', () => {
    expect(new NnrShopValidators()).toBeTruthy();
  });
});
